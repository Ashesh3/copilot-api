/var/log/nginx/access.log /var/log/nginx/error.log /var/log/nginx/*copilot-api*.log {
  daily
  size 50M
  rotate 7
  compress
  delaycompress
  missingok
  notifempty
  sharedscripts
  postrotate
    test ! -r /run/nginx.pid || kill -USR1 $(cat /run/nginx.pid)
  endscript
}
