name: Nightly Smoke Tests

on:
  schedule:
    - cron: "0 0 * * *" # midnight UTC
  workflow_dispatch: # manual trigger

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies & build
        run: bun install && bun run build

      - name: Start copilot-api
        run: |
          bun run dist/main.js start -g "$GH_TOKEN" --host 0.0.0.0 &
          echo "Waiting for server..."
          # Wait for server to be ready and models to load
          for i in $(seq 1 30); do
            if curl -sf http://localhost:4141/v1/models > /dev/null 2>&1; then
              echo "Server is up (models loaded)"
              break
            fi
            echo "  attempt $i/30..."
            sleep 3
          done
          # Final check
          curl -sf http://localhost:4141/v1/models > /dev/null || { echo "Server failed to start"; exit 1; }
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Install CLI tools
        run: |
          npm i -g @anthropic-ai/claude-code@latest &
          npm i -g @openai/codex@latest &
          npm i -g @google/gemini-cli@latest &
          wait
          echo "--- Installed versions ---"
          claude --version || echo "claude not found"
          codex --version || echo "codex not found"
          gemini --version || echo "gemini not found"

      - name: Run smoke tests
        run: |
          chmod +x tests/smoke/run-smoke-tests.sh
          tests/smoke/run-smoke-tests.sh http://localhost:4141
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Upload test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-test-logs
          path: tests/smoke/*.log
          retention-days: 7
          if-no-files-found: ignore
